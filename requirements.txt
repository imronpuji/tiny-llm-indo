# Core ML libraries
torch>=2.2.0
transformers>=4.38.0
datasets>=2.14.0
tqdm>=4.65.0
accelerate>=0.26.0
huggingface-hub>=0.16.0

# Qwen2.5 fine-tuning (LoRA + QLoRA)
peft>=0.8.0
bitsandbytes>=0.42.0
trl>=0.7.0

# Optional
# triton>=2.1.0   # GPU optimization (Linux only)
